/*
class
MouseHandler {
private:
	Camera& camera;
	float prevX;
	float prevY;

public:
	MouseHandler(Camera& _camera);
	static void mouseCallback(GLFWwindow* window, double xpos, double ypos); // Must Be Static C Func Ptr
	void shiftCameraTarget(const float xpos, const float ypos);

};
*/
